services:
  server:
    build:
      context: ./src/server
    ports:
      - "8200:8200"

  client:
    build:
      context: ./src/client
    ports:
      - "5273:5173"
    environment:
      VITE_API_BASE_URL: http://127.0.0.1:8200