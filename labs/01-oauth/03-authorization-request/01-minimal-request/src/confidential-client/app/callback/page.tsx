export default function CallbackPage() {
  return (
    <main>
      <h1>OAuth Callback</h1>

      <p>
        Authorization response handling is intentionally
        deferred to a later experiment.
      </p>
    </main>
  );
}